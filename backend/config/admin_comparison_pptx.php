<?php

/*
|--------------------------------------------------------------------------
| 依頼BG(2026-09-08): 多社比較スライドを、営業資料(PPTX)へ差し込む機能
|--------------------------------------------------------------------------
| App\Services\Report\AdminComparisonPptxInserter が使う。既存のPPTX(1診断
| 1件のAnalysisAttachment)を PhpOffice\PhpPresentation で開いて保存し直す
| ことは絶対にしない(グラフ・埋め込みブック・ノート・テーマが失われるため、
| 依頼BG-1の必須要件) ―― ZipArchiveで既存パーツをそのまま保ち、スライド1枚
| ぶんのパーツだけを追加する。
*/

return [

    /*
    |--------------------------------------------------------------------------
    | 「参照元」ページを見つけるための語(コードに直書きしない)
    |--------------------------------------------------------------------------
    | 各スライドのテキストからこの一覧のいずれかの語が見つかったスライドを
    | 「参照元」ページとみなし、その直前に比較スライドを差し込む。末尾側
    | (最後のスライド)から探し、最初に見つかったスライドを採用する。
    | 見つからない場合は差し込まずエラーにする(位置を推測しない、依頼者指定)。
    |
    | 依頼BH-1(2026-09-08): 単独の「出典」を一覧から外した。共有された実物
    | 3本(三井不動産・日鉄ソリューションズ・楽天モバイル)を実測した結果、
    | 3本とも本文ページ(グラフ・数値の注記)に「出典」が普通に出現しており、
    | 末尾から探す設計のおかげで先に最終ページの「参照元」/「APPENDIX」に
    | 当たっていたためたまたま誤挿入していなかっただけだった。最終ページに
    | 「参照元」も「APPENDIX」も無い資料が来た場合、「出典」が本文ページで
    | ヒットしてしまい、本来は中止すべきところが本文の途中への誤挿入に
    | なりかねない(依頼BGで禁止した「位置を推測して差し込むこと」が、この語
    | のせいで機能しなくなる)。「出典」を外しても、実物3本はいずれも
    | 「参照元」と「APPENDIX」の両方を最終ページに含むため検出結果は
    | 変わらない(依頼者による実測で確認済み)。本文中の「出典：」に誤爆
    | しない複合語(「出典と注記」等)を足すのは問題ない。
    */
    'reference_page_keywords' => array_values(array_filter(array_map(
        'trim',
        explode(',', (string) env(
            'ADMIN_COMPARISON_PPTX_REFERENCE_PAGE_KEYWORDS',
            '参照元,APPENDIX',
        )),
    ))),

    /*
    |--------------------------------------------------------------------------
    | 挿入を許可するスライドサイズ(EMU)
    |--------------------------------------------------------------------------
    | AdminComparisonPptxGeneratorが生成するスライドと同じ16:9(13.333×7.5in)。
    | 差し込み先のppt/presentation.xmlのp:sldSzがこの値と一致しない場合は
    | 差し込まずエラーにする(依頼者指定 ―― 4:3のデッキに16:9のスライドを
    | 入れるとはみ出すため)。
    */
    'required_slide_width_emu' => (int) env('ADMIN_COMPARISON_PPTX_REQUIRED_SLIDE_WIDTH_EMU', 12192000),
    'required_slide_height_emu' => (int) env('ADMIN_COMPARISON_PPTX_REQUIRED_SLIDE_HEIGHT_EMU', 6858000),

    /*
    |--------------------------------------------------------------------------
    | スライドサイズの許容差(EMU、幅・高さそれぞれに適用)
    |--------------------------------------------------------------------------
    | 依頼BK(2026-09-09): 本番で16:9の実物の資料(cx=12191695、cy=6858000。
    | 要求値との差はcxのみ305EMU)が「16:9でない」として弾かれた。cmで
    | スライドサイズを指定するツール・書き出し経路の丸め等により、見た目には
    | 判別できないごく小さなEMUのずれが実在するため、完全一致ではなく
    | 許容差以内かどうかで判定する。
    |
    | 依頼BL-2(2026-09-09): 当初1200EMUにしたが、cmで「33.87」と指定した
    | 資料(12193200EMU、要求値との差がちょうど1200)が境界ぎりぎりで、
    | 余裕が無かった(比較演算子が変わる・許容差が1199になる等で弾かれ得る)。
    | 9144EMU(=0.01インチ)に広げた。根拠:
    | - cm指定由来のずれ(1200EMU差の12193200)が、境界ではなく約7.6倍の
    |   余裕をもって通る(9144-1200=7944の余白)
    | - 実測した本番資料のずれ(305EMU)の約30倍の余裕
    | - 4:3(9144000×6858000)との差はcxだけで3,048,000EMU、16:10
    |   (9144000×5715000)との差はcyだけで1,143,000EMU ―― どちらも
    |   9144EMUの125倍以上あり、9144EMUまで許容しても4:3・16:10を通す
    |   余地は無い(4:3は333倍、16:10は125倍の余裕)
    | - 実際に9144EMUずれた資料(両軸とも紙面が小さくなる方向、最悪ケース)を
    |   比較スライドの差し込み対象として画像化し、絶対座標で置かれた
    |   比較スライドの右端・下端に見た目のずれが出ないことを確認済み
    |   (依頼BL-2の報告を参照。比較スライド自体が右0.93in・下1.3inの
    |   余白設計のため、0.01インチのずれはこの余白の1%程度にしかならない)
    |
    | 【この値と、直下のaspect_ratio_toleranceは別の設定であり、兼用しない
    | (依頼者指定)】この値は「同じ紙面サイズか」(絶対寸法の一致)を、
    | aspect_ratio_toleranceは「同じ形か」(縦横比の一致)を判定する ――
    | 意味が異なるため、片方を変えてももう片方に影響しない。
    */
    'slide_size_tolerance_emu' => (int) env('ADMIN_COMPARISON_PPTX_SLIDE_SIZE_TOLERANCE_EMU', 9144),

    /*
    |--------------------------------------------------------------------------
    | 縦横比の許容差(比率の差、単位なし)
    |--------------------------------------------------------------------------
    | 依頼BL-1(2026-09-09): エラー文言に添える比率名(4:3/16:9/16:10)を、
    | 「既知の絶対サイズとの一致」ではなく「cx/cyの比そのもの」で判定する。
    | 従来は絶対サイズの表(例: 16:9 = 12192000×6858000)だけを見ていたため、
    | 同じ16:9でも別のインチ数の資料(例: 9144000×5143500、10×5.625in)には
    | 比率名が付かず、「この資料は25.40 × 14.29 cmです。16:9（33.87 ×
    | 19.05 cm）の資料が必要です。」という、依頼BKで直したのと同種の
    | 「16:9ではないと言われたが、これは16:9である」という混乱を生んでいた。
    |
    | 0.001にした根拠:
    | - 4:3(1.333333)・16:10(1.6)・16:9(1.777778)の隣り合う比率どうしの
    |   差は最小でも0.177778(16:10と16:9の間)あり、境目はその半分の
    |   0.088889 ―― 0.001はそれの1/89しかなく、3つの比率が混ざる余地は無い
    | - 実際に確認した資料の比のずれ(12193200/6858000と16/9の差は約0.00007、
    |   12191695/6858000との差は約0.00005)の10倍以上の余裕がある
    | - 9144000×5143500(10×5.625in)は16/9とちょうど一致する(比のずれ0)ため、
    |   0.001の許容差があれば確実に16:9と判定される
    |
    | 【slide_size_tolerance_emuとは別の設定(依頼者指定、上記参照)】
    */
    'aspect_ratio_tolerance' => (float) env('ADMIN_COMPARISON_PPTX_ASPECT_RATIO_TOLERANCE', 0.001),

    /*
    |--------------------------------------------------------------------------
    | 比較スライドで、領域名の下(または脇)に添える短い補足
    |--------------------------------------------------------------------------
    | 依頼BM-1(2026-09-09): config('brand_wheel.axes')のキーと1対1で対応する。
    | 領域名(name_ja)だけでは何を指しているか伝わりにくいため、共有された
    | モックアップ(comparison_slide_v2.html)の補足文言をそのまま採用する
    | (依頼者提案依頼への回答として、モックアップの文言を提案とする)。
    |
    | 依頼CB-1(2026-09-24): 旧「領域別の発信量」マトリクス(依頼BM〜BQ)は
    | ブランド・ホイール比較スライド(ヘキサゴン)に置き換わったが、この
    | 補足自体は各社ヘキサゴンの頂点ラベル/凡例(6領域それぞれの数値を
    | 読み取れるようにする、CB-1必須要件)で引き続き使う。
    |
    | brand_wheel.php本体には置かない ―― この補足はこの営業資料差し込み
    | だけの表示用であり、リード向けレポート等、config('brand_wheel')を
    | 参照する他の画面には一切使わない(依頼者指定、直書き禁止の対象は
    | コード・Bladeであり、この補足自体を持つ場所はここが適切と判断)。
    */
    'axis_captions' => [
        'will_activity' => '事業・商品・成長性',
        'asset' => '規模・実績・ブランド',
        'personality' => '理念・組織・意思決定',
        'relationship' => '働き方・制度・場所',
        'emotional_benefit' => 'やりがい・人・風土',
        'financial_benefit' => '報酬・福利厚生・成長機会',
    ],

    /*
    |--------------------------------------------------------------------------
    | 「競合が伝えていて、自社が伝えていない項目」一覧の表示件数上限
    |--------------------------------------------------------------------------
    | 依頼BO-1で導入。依頼CB-4(2026-09-24)で、この一覧の置き場所が「まとめの
    | 帯の下に付け足したカード」(旧addSummaryAndMissingItemsCard、まとめの帯の
    | 残り高さに依存する可変上限だった)から、専用の1枚のスライド
    | (AdminComparisonPptxGenerator::generateMissingItemsSlide())に変わった
    | ―― 旧「まとめ」の帯・6領域マトリクスの比較スライドはCB-1のブランド・
    | ホイール比較スライドに置き換わり、このクラスからbuildSummary()ごと
    | 削除した(旧summary_templates/gap_axis_list_thresholdも不要になった
    | ため削除)。
    |
    | 依頼CC-2①(2026-09-25): 本番で出力した資料を実物で確認したところ、
    | 候補者調査の行(3行目)に、行の区切り線が突き抜けて見える不具合が
    | あった(依頼者指摘 ―― 依頼CBの報告では「直した」としていたが、実際の
    | 出力には残っていた)。原因は各行内の3ブロック(項目名/一文/候補者調査)
    | の高さの見積もりが、実際のMeiryoの行送りより小さすぎたこと(自動テスト
    | では気づけない、実機画像化で目視して初めて分かった)。各ブロックの
    | 高さを実測ベースの値に引き上げ、区切り線も行の直下ではなく行間の
    | 余白の中央に置き直した(addMissingItemsRows参照)。1件あたりの高さが
    | 0.62in→0.84inに増え、冒頭の説明文(missing_items_intro、新規)ぶんの
    | 縦も必要になったため、上限を6→4へ引き下げた。タイトル・見出し・
    | 説明文(上部約2.3in)と出典・footer(下部約1.2in)を除いた残り約3.9inに、
    | 1件あたり0.84inで4件収まり、区切り線が文字と重ならないことを実機
    | 画像化で確認した。
    */
    'missing_items_max_count' => 4,

    /*
    |--------------------------------------------------------------------------
    | 「足りないもの」スライド(CB-2)の各行:「伝わっていないと何が起きるか」
    |--------------------------------------------------------------------------
    | 依頼CB-2: 捏造した文言ではなく、config('brand_wheel.axes.*.
    | sub_element_definitions')(その項目が何を指すかの定義、既存・既に
    | AIプロンプトやレポートで使われている確定済みの文言)をそのまま使う。
    | %1$s に定義文をそのまま埋め込む(文言を書き換えない)。
    | AdminComparisonPptxDataBuilder::buildMissingItems()参照
    | (missingFromSelf側のdefinition/recommendationフィールドは
    | 依然として読まない ―― config('brand_wheel.axes')から直接引く)。
    |
    | 定義文は既に句点(「。」)で終わる(複数文のものもある、例:
    | 「会社が何を目指しているか、存在意義や志を述べた記述。取扱商品・
    | サービス内容の説明のみでは該当しない。」)。「%sが、」のように読点で
    | 続けると「…しない。が、自社サイトでは…」という句点の直後に「が」が
    | 来る不自然な文になる(実機画像化で発覚)。読点でつながず、定義文の
    | 直後を独立した1文として続ける。
    */
    'missing_item_impact_template' => '%s自社サイトでは確認できていません。',

    /*
    |--------------------------------------------------------------------------
    | 「足りないもの」一覧の見出し・0件時の文言(依頼BO-1、依頼BQ-1/BQ-3で改訂)
    |--------------------------------------------------------------------------
    | 依頼BQ(2026-09-11)で判明: 抽出条件は「競合の少なくとも1社」ではなく
    | 「競合の過半数」(BrandWheelMultiSiteComparisonComposer::
    | extractMissingFromSelf()、依頼AC由来、多社比較PDFと共有、この依頼では
    | 変更しない)。旧見出し「競合が伝えていて、自社が伝えていない項目」は
    | 「1社でも言及していれば挙がる」と読めてしまい、実際の条件(過半数)と
    | 食い違っていた(依頼者指摘)。
    |
    | 過半数の実際の人数(競合N社中M社以上)を文言に出す判断(依頼者へ提案・
    | 採用): 「過半数」とだけ言うより、営業の場で「3社中2社」と具体的な
    | 数字を出す方が反論しにくい(依頼者の背景説明と同じ理由)。多社比較PDF
    | (admin-comparison-pdf.blade.php)の既存の文言「競合{N}社中{M}社以上が
    | 言及しているにもかかわらず、自社サイトでは確認できなかった項目です。」
    | と同じ「N社中M社以上」の言い回しに合わせた(PDF側は変更していない、
    | 表現の一貫性のためスライド側をこれに合わせただけ)。
    |
    | %1$d=競合社数、%2$d=過半数の人数(BrandWheelMultiSiteComparisonComposer::
    | majorityThreshold()から算出、AdminComparisonPptxDataBuilder参照。
    | 競合3社なら2社、5社なら3社)。
    |
    | 0件(自社が24項目すべてを満たす)のとき、セクションごと消して余白を
    | 残すことは禁止(依頼BO由来、依頼者指定) ―― 見出しの下にこの文言を出す。
    */
    'missing_items_heading' => '競合%1$d社中%2$d社以上が伝えていて、自社が伝えていない項目',
    'missing_items_empty_text' => '見つかりませんでした。',

    /*
    |--------------------------------------------------------------------------
    | 依頼CC-2(2026-09-25): 「足りないもの」スライド冒頭の説明
    |--------------------------------------------------------------------------
    | 依頼CB-2の報告では各行に「候補者調査：「〇〇」を重視する求職者　N%」と
    | だけ出しており、何と何を突き合わせているかが書かれていなかった
    | (依頼者指摘、実物のPPTXで確認)。ページ冒頭にこの一文を置く。
    */
    'missing_items_intro' => '自社サイトから確認できた発信内容を、求職者への調査結果と突き合わせています。'
        .'以下は、求職者の関心が高いにもかかわらず、自社サイトでは確認できなかった項目です。',

    /*
    |--------------------------------------------------------------------------
    | 依頼CC-2①②(2026-09-25): 各行の候補者調査行
    |--------------------------------------------------------------------------
    | 旧文言「候補者調査：「〇〇」を重視する求職者　N%」は、何と何の
    | 因果関係かが分からなかった(依頼者指摘)。「この情報を探している
    | 求職者がN%いるが、自社サイトでは確認できなかった」という順に書き直す。
    | %1$s=候補者調査の項目名、%2$s=割合(数値のみ、%は文言側に含む)。
    |
    | 対応表(config('brand_wheel_candidate_survey'))で「該当なし」の項目は
    | missing_item_survey_none_textを使う ―― 「調査の選択肢に対応するものが
    | 無かっただけで、重要でないという意味ではない」ことが伝わる一言を添える
    | (依頼者指定、必須)。
    */
    'missing_item_survey_template' => '「%1$s」を確認したい求職者が%2$s%%いますが、自社サイトでは確認できませんでした。',
    'missing_item_survey_none_text' => '候補者調査の選択肢には対応する項目がありませんでした(重要でないという意味ではありません)。',

    /*
    |--------------------------------------------------------------------------
    | 「自社サイトの階層図」スライド(CB-3)
    |--------------------------------------------------------------------------
    | 依頼CB-3: analysis_crawled_pagesのdiscovered_via('link'|'sitemap')には
    | 親子関係(どのページから見つかったか)が記録されていない
    | (依頼者指定、スキーマは変更しない)ため、リンク構造から木を組み立てる
    | ことはできない。代わりにURLのパス階層(起点URLからの相対パス
    | セグメント)から1階層目の枝だけを組み立てる
    | (AdminComparisonSiteHierarchyBuilder参照)。depth列(seedからのBFS
    | ホップ数)は使わない ―― サイトの階層とは一致しないため(依頼者指定)。
    */

    // 1階層目の枝の表示上限。超えた分は「ほかN」に畳む。
    // 6にした根拠: 1枝あたり[枝名+ページ数]1行+代表ページ最大3件を1行に
    // まとめた表示で、タイトル・TOP行・footer(巡回範囲の注記)を除いた
    // 残り約4.5inに、1枝あたり約0.7inで6枝収まることを実機画像化で確認した
    // (missing_items_max_countと同じ考え方、CB-2の一覧と縦の使える幅が
    // 近いスライド構成のため近い値になっている)。
    'site_hierarchy_branch_limit' => 6,

    // 1枝につき示す代表的なページ名の件数上限。3件あれば「何が並んでいる
    // 枝か」を判断するのに十分で、1行に収まる(実機画像化で確認)。
    'site_hierarchy_sample_pages_per_branch' => 3,

    // 巡回した範囲に1階層目の枝が1件も無かったとき(依頼BO由来、セクション
    // ごと消して余白を残さない)。%sには起点URLを埋め込む。「ありません」
    // と断定しない(依頼者指定、巡回は上限ページ数までのため存在しないとは
    // 言い切れない)。
    'site_hierarchy_empty_text' => '巡回した範囲では、%sの配下にページが見つかりませんでした。',

    // 「足りないもの」(CB-2)に対応するサイトの導線名を、追加を推奨する
    // 導線として並べる際の見出し。「ありません」ではなく「推奨」の文脈で
    // 出す(依頼者指定、CB-3必須事項)。
    'site_hierarchy_recommended_heading' => '追加を検討したい導線',

    // 巡回の範囲についての注記(依頼者指定、必須)。%dにはconfig
    // ('brand_wheel.crawl_max_pages')(巡回の上限ページ数)を埋め込む。
    // 大きいサイトでは巡回できるのが全体の一部(実測: NTTデータ603ページ中
    // 50ページ=8%)であり、「無い」と断定できないことを明示する。
    'site_hierarchy_crawl_scope_note' => '巡回は最大%d件のページまでを対象としています。サイト全体の一部のみを確認した結果であり、'
        .'ここに記載の無い項目が自社サイトに存在しないとは限りません。',

    /*
    |--------------------------------------------------------------------------
    | 依頼CC-3①(2026-09-25): 起点URLの外にあったページ数
    |--------------------------------------------------------------------------
    | 本番の実例(NTTデータ): 巡回した50件のうち、起点URL(採用ページ)配下に
    | あったのはcareers/uptodataの3ページだけで、残り47件は許可ホスト
    | (ホスト単位、crawl_domain_scope=exact_host)の範囲内だが起点パスの
    | 外(IR・ニュース等の会社サイト側)にあった。この事実が階層図のどこにも
    | 書かれておらず、「階層図が薄い」ことの理由が誰にも分からないまま
    | 資料に出ていた(依頼者指摘)。
    |
    | 警告ではなく事実として1行添える(依頼者指定 ―― 商談で相手に見せる
    | スライドであり、警告調の見た目にしない)。%1$d=巡回した全件数
    | (status=fetched)、%2$d=そのうち起点URL配下だった件数。
    | AdminComparisonSiteHierarchyBuilder::build()が実データから算出する
    | (数値を捏造しない)。
    */
    'site_hierarchy_scope_note' => '巡回した%1$d件のうち、この起点URL配下にあったのは%2$d件でした。残りは同じドメインの別のセクションです。',

];
